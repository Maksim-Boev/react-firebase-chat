import React, { FC } from "react";
import { Container, Grid } from "@material-ui/core";

const Login: FC = () => {
  return (
    <Container>
      <Grid container />
    </Container>
  );
};

export default Login;
